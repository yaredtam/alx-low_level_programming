#include "main.h";

/**
 * _isupper - checks if variable c is in uppercase
 * @c: int to be checked
 * RETURN: 1 if c is uppercase, 0 otherwise 
 */

int _isupper(int c)
{
	return (c >= 65 && c<=90);
}
